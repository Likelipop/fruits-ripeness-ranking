[project]
name = "fruits-ripeness-ranking"
version = "0.1.0"
description = "Fruit ripeness ranking with transfer learning and quantization"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "torch>=2.0.0",
    "torchvision>=0.15.0",
    "pillow",
    "numpy",
    "pandas",
    "tqdm",
    "ruff>=0.15.16",
]
